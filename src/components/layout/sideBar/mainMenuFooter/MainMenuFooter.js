import React from "react";
import Box from "@mui/material/Box";
import Divider from "@mui/material/Divider";

const MainMenuFooter = () => {
  return (
    <Box sx={{ mt: 'auto' }}>
      <Box sx={{ p: 2 }}>
        <Divider />
        Main Menu footer options
      </Box>
    </Box>
  );
};

export default MainMenuFooter;
